<?php

$db_name="";			// insert the name of the databse OW should use on your MYSQL installation
$db_user="";			// insert the name of the user which OW should use to access the DB
$db_password="";		// insert the password which OW should use in connection with the user to connect to the DB

$db_host="localhost";	// if your DB is hostet on a different server, enter its adresse here; if not just leave the default 'localhot'

?>